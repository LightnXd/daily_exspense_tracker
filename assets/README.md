Place your launcher icon at `assets/logo.png` (recommended 1024x1024 PNG).

After adding `assets/logo.png`, run:

```
flutter pub get
flutter pub run flutter_launcher_icons:main
```

This will generate Android and iOS launcher icons using that image.
